{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "statusMessage": "Running fmt + clippy checks...",
            "command": "cmd=$(python3 -c \"import sys,json; d=json.load(sys.stdin); print(d['tool_input']['command'])\"); if echo \"$cmd\" | grep -qE '^git commit'; then source ~/.cargo/env 2>/dev/null; cargo fmt --check && cargo clippy -- -D warnings; fi"
          }
        ]
      }
    ]
  }
}
